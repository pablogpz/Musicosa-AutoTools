import { EditionProps } from './Template'

export const defaultEditionProps: EditionProps = {
    scoreMinValue: 0,
    scoreMaxValue: 10,
    sequenceNumberInAuthorEntries: [1, 2],
    avgScoreDelta: 0.5
}